<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@600;700&display=swap" rel="stylesheet">
    <title>NoteIt</title>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900 mx-auto p-4">
        <h1 class = "px-10 text-4xl font-semibold tracking-tight text-yellow-500 drop-shadow-sm font-['Montserrat']">Welcome to NoteIt !</h1>
        
        <div class="form-container p-10">
            <form action="/create" method="POST">
                <input class="block px-5 py-2 text-zinc-100 bg-zinc-800 border border-zinc-700 p-2 w-full mb-2"
                    type="text" name="title" placeholder="Note Title">
                <textarea
                    class="block px-5 py-2 text-zinc-100 bg-zinc-800 border border-zinc-700 p-2 w-full mb-2 resize-none"
                    name="details" placeholder="Note Content"></textarea>
                <button type="submit" class="bg-blue-500 text-white px-4 py-2">Add Note</button>
            </form>
        </div>

        <div class="tasks p-10 flex flex-wrap gap-4 w-full">
            <% if(files.length> 0) { %>
                <% files.forEach(function(file) { %>
                    <div
                        class="task w-72 px-4 py-2 bg-zinc-800 border border-zinc-700 rounded-lg mb-2 rounded-lg text-zinc-100">
                        <h1 class="text-2xl tracking-tighter font-semibold text-yellow-300"><%= file.split(".txt")[0] %></h1>
                        <div class="flex w-full justify-between">
                        <a href="/files/<%= file %>" class="inline-block mt-1 text-blue-400 hover:text-blue-300">View Note</a>
                        <a href="/edit/<%= file %>" class="inline-block mt-1 text-green-400 hover:text-green-300">Edit</a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                    <h3 class="text-zinc-500">No notes available</h3>
            <% } %>
        </div>

    </div>
</body>
</html>